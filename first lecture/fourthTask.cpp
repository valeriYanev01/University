#include <iostream>
using namespace std;

int main()
{
    cout << "Tokens:\nkey words ID's integers constants strings operators delimeters.";
    cout << "\nLiteral: they are fixed values which can be integers, floating integers, string literals and character literals.";
    cout << "\nCommands: they are not part of the program, they just control it's workflow; they compile, connect and replace text";
}